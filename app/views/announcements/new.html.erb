<div class="n-container">
  <h1 class="n-page-title">お知らせ新規登録</h1>

  <%# エラーメッセージがある場合に表示 %>
  <% if @announcement.errors.any? %>
    <div style="color: red; margin-bottom: 20px;">
      <h2><%= @announcement.errors.count %>件のエラーがあります。</h2>
      <ul>
        <% @announcement.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%# 登録用フォーム %>
  <%= form_with(model: @announcement, local: true) do |form| %>
    <div style="margin-bottom: 15px;">
      <%= form.label :title, 'タイトル', class: "n-title" %>
      <%= form.text_field :title, style: "width: 100%; padding: 8px; box-sizing: border-box;" %>
    </div>

    <div style="margin-bottom: 20px;">
      <%= form.label :content, '本文', class: "n-title" %>
      <%= form.text_area :content, rows: 5, style: "width: 100%; padding: 8px; box-sizing: border-box;" %>
    </div>

    <div>
      <%= form.submit '登録する', class: "btn-primary" %>
      <%= link_to '戻る', announcements_path, class: "n-back-btn" %>
    </div>
  <% end %>
</div>